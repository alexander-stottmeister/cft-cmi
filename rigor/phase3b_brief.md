# Phase 3b brief — the quasi-free SDP certificate (tracks S10, S11), 2026-09-10

## Context (read first)
- `rigor/optimality_second_order.tex` (S8): the second-order recovery problem over gauge-invariant quasi-free
  channels is the convex programme  min_{(X,Z) in F} g_Q(Q - Q_rec(X,Z)),
  F = {X: h_B -> h_D, Z=Z^* on h_D : X Q_BB X^* <= Z <= 1 - X(1-Q_BB)X^*},
  Q_rec = [[Q_AA, Q_AB X^*],[X Q_BA, Z]],  g_Q(delta) = 1/4 sum_ik w_ik |delta~_ik|^2 (S8 eq. (3.9), SLD form).
- `rigor/optimality_all_channels.tex` (orchestrator, today): for EVERY recovery channel the recovered symbol lies
  in F (Thm 3.5) and -log F >= g_Q(defect)(1-o(1)) (Prop 2.4, Thm 4.1). So optimality of the geometric
  compression among ALL channels is EQUIVALENT to: the programme above has second-order value f_2 z^2,
  f_2 = c/(12 pi^2), attained by the zero-collar compression
  (X_c = one-particle map of the Moebius dilation h_lambda: D=(0,L) -> B=(0,b), (X_c g)(x) = sqrt(h'(x)) g(h(x))
  appropriately; Z_c = X_c Q_BB X_c^*, i.e. Y = 0 and X_c X_c^* = 1: BOTH constraints active).
- Dual form (Thm 4.1(3), Sion): value = sup_t Delta(t)_+^2 / (8 V(t)),  Delta(t) = inf_F Tr t (Q - Q_rec),
  V(t) = Tr(t Q t (1-Q)).  KKT: the compression is optimal iff, with t_c := SLD of delta_c = Q - Q_rec(X_c,Z_c)
  (t_c = 1/2 sum w_ik delta~_ik |i><k| in the Q-eigenbasis), (X_c, Z_c) MAXIMISES the linear functional
  Tr(t_c Q_rec(X,Z)) over F.  Useful reduction for fixed t: sup over Z in the operator interval gives
  sup_{||X||<=1} [ 2 Re Tr(t_DA Q_AB X^*) + Tr(t_DD X Q_BB X^*) + Tr(((1-XX^*)^{1/2} t_DD (1-XX^*)^{1/2})_+) ].
- Continuum data: Note 5 (`fidelity_recovered_quasifree.tex`) modular frame diagonalising Q_DD; P2's
  `quadratic_limit.tex` Thm 4.2 computes g_Q(delta_c) = f_2 zeta^2 via the Legendre form (the SLD of the
  compression defect is essentially the generator of the C^{1,1} flow: delta_c = s [K, Q]-type at first order,
  K the one-particle generator of -x^2 d/dx on D, 0 on A; see `uhlmann_upper_bound.tex` Secs 3-5).

## S10 (analytic): prove or refute "the compression is the optimum of the quasi-free programme at second order".
Deliverable: `rigor/sdp_dual_certificate.tex` (Lamport style, `\input{rigor_preamble}`; create the file within
your first 3 tool calls; append <= 80 lines per call; pdflatex after each append). Routes, in order:
 1. KKT at the compression: show the linear functional Tr(t_c Q_rec) is maximised over F at (X_c,Z_c). Both
    constraints are active; the normal cone of F at a point with Y=0 and XX^*=1 is generated by the two LMI
    multipliers. Work at leading order in s (defect O(s)), where everything is a first-variation computation.
 2. Alternatively exhibit ANY family t_z with Delta_z(t_z)^2/(8 V(t_z)) >= f_2 z^2 (1+o(1)). Try t_c itself and
    compute Delta(t_c) with the reduction above.
 3. If neither closes, produce the sharpest rigorous lower bound you can (e.g. the t_DD=0 family gives
    E >= sup_s [Re Tr(s Q_AD) - ||s Q_AB||_1]^2 / (2 V(t_s)), t_s=[[0,s^*],[s,0]]) and evaluate it.
State clearly what is proved and what is not (verdict boxes). Findings entries: plain LaTeX only (README_AGENTS).

## S11 (numerical): verify the KKT structure and evaluate the dual value.
 (a) Lattice: with `numerics/optimality/best_4_8_2.npz` (S8's optimal (X,Z)) compute t_opt = SLD of the
     defect and check that maximising Tr(t_opt Q_rec) over F (a small SDP; cvxpy+Clarabel are in the venv,
     see `numerics/optimality_all/*.py` for conventions) returns the optimum itself (KKT sanity), and compute
     Delta(t_opt)^2/(8V) vs g_Q at the optimum (should agree: strong duality).
 (b) Continuum: discretise the modular frame of Note 5 (`numerics/compression_box.py` has the box
     discretisation of the compression defect) and compute, at small s, (i) the SLD t_c of the compression
     defect, (ii) Delta(t_c) by solving the linear programme over F (reduction above) numerically,
     (iii) the ratio Delta(t_c)^2/(8 V(t_c)) / (f_2 zeta^2). If the ratio -> 1 the certificate is t_c; if < 1
     report the maximiser (which (X,Z) beats the compression for the functional).
 Deliverable: `numerics/optimality_all/kkt_*.py` + `.out`, and a section appended to
 `rigor/optimality_all_channels.tex`? NO — write your own `rigor/sdp_certificate_numerics.tex` (Lamport
 conventions, compile after each append, create within 3 tool calls).
Venv: /private/tmp/claude-501/-Users-alex-Documents-Uni-Hannover-claude-team/2a3c5aab-2bd9-408c-811f-f4144c2149e4/scratchpad/venv/bin/python
Rules: write to disk early; never exceed ~100 lines per tool call; report honestly; no unicode in .tex math.
