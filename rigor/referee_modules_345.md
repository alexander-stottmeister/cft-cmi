# REF-MOD345 — site modules 3, 4, 5 (2026-09-21) — PASS, seven repairs

Scope: `docs/explore/{universality,optimality,theta-reconciliation}.html`, `build_universality()` and the three new `build_theta()` groups. Modules 7-10 and their extractor were NOT reviewed. Method: a jsdom run of each page (boot, every control, arrow keys, theme, 358 px), nine guard-break tests in a symlink shadow tree, and a record-by-record provenance audit. Nothing in the repository was modified.

**Module 5 — the confound is gone.** Run, not read. Control 1 varies the window at FIXED width 0.12 and plots theta at L = 256, 512, 768, monotone at each L (.27762/.28958/.36178, .28986/.29511/.32313, .29569/.29898/.31573); it is BOUNDED at upper edge 0.47 (`iwin` max=2; setting 5 clamps to 2) AND carries the |u|^2 panel with the excluded (0.38,0.50) point at 0.024005 and the untapered level for scale — both remedies REF-P0-TAPER allowed, not one. Control 2 is separate, shows |u|^2 only and says in bold why: the fixed-edge theta scan has not been run; the three scattered theta values are named as three points, with the sentence that running the scan is what would replace the axis. The five-window sweep is an exhibit with each width printed and marked `(same)`/`(different!)`, and nothing downstream reads it as a scan. The untapered model is shown stable in L (|u|^2 .014889/.014912/.014921) and answering a different question, Fermi sea doubled at 2.016x; d theta/d log L = 0.00876 is computed live and labelled with its range L = 256..1024.

**Module 3.** Measured points are drawn only at c = 1 and never move: at c = 3 the three measurement circles keep cx = 201.90 while only the theorem handle moves to 465.71. Every readout says "measured at c = 1"; the note names the absence of any other-c measurement; the g_B view draws no points and says why.

**Module 4.** `all-channel-optimum-value`, `tangent-problem-exact-parametrisation`, `kkt-noise-sign-criterion` and `kkt-noise-criterion-numerics` carry conditional badges naming H1/H2/H3, H1/H2, H1/H2/H3 and H3. All 24 badges on the three pages match the KB status exactly; `numerical` uses its own tokens, never `proved`'s.

**Numbers, guards, contracts.** 125/125 in-scope records verify against the file and line they name; I re-checked F3_RESULTS.md 15, 153-158, 163, 191, 203, 204 and all 21 lines of f3_circ_taper2.out by hand. No record comes from `docs/results/`. `renderRecord` throws on a sourceless record and all three pages then drop `has-js` and fall back to the static view — tested by stripping `source`, by deleting records, and with a 503. `--check` is byte-exact, exit 0. Nine guard-break tests all raised Drift: the g_B and W-hat boxed closed forms, the <TT> prefactor, both halves of the lattice `zeta = 2z` convention, the kappa_c column list, the width-0.12 sub-sweep, the edge-0.49 width list, and the circle model's `a`. Contracts met: prose plus a real static figure with JS off; `has-js` removed in all six catches; one external origin (the repository blob root) and no runtime fetch beyond `../data/*.json`; KaTeX 0.18.7 vendored with all 20 woff2 faces resolving; role=slider with tabindex and a full aria set refreshed on every render, focus restored through `data-handle-key`; both theme paths; no page overflow at 390 px; a skip link. The author's four admissions are all handled in the page rather than papered over — including `spectral-model-uv-pollution`, which has no results page and links `status.md#area-optimality` (anchor at line 86, claim at line 120) marked "(no page of its own)".

## Repairs required
- **R1** `theta-reconciliation.html:273` paints the literal `the control stops here " + ARR + "` onto the theta plot — a broken concatenation inside single quotes, on the callout that explains the bound. Replace with `'the control stops here →'`.
- **R2** `theta-reconciliation.html:100` says "Five windows"; `sweep()` renders seven rows, the two untapered controls arriving with `—` and no marker. Filter `window === 'none'`, or say "five windows, and the two untapered controls beneath them".
- **R3** `build_site_data.py:1096` calls `check_page_records(Path("docs"), ...)`; from any cwd but the root it scans zero pages and prints "all resolve". Use `ROOT / "docs"`.
- **R4** The head `window 'error'` handler (line 15 of each page) never removes `has-js`, so after `enableJS()` its panel promises the static figure while CSS hides it. Add the `remove('has-js')` line, and register the listener with `true` so a blocked module load reaches it at all.
- **R5** The all-channel optimum 0.616 shows a bare `numerical` badge in three readout cells; give it the `badge('numerical', ['H1','H2','H3'])` treatment the KKT panel already uses.
- **R6** All four interactive SVGs carry `role="img"`, whose children are presentational, so the aria values may never be announced. Drop it on `#line`, `#gain`, `#relax`, `#sharp` and move the text into `<title>`.
- **R7** On `#sharp` the arrow keys drive `aria-valuenow` backwards — ArrowRight decreases it and is dead at the default position. Reverse the array or map the index to the width.

## Inherited, and what the remaining modules must not copy
R3, R4 and R6 are P3's shared layer (`geometry.html:119` has the same `role="img"` over a drag handle). Also shared and unfixed: the static figure carries its own `prefers-color-scheme` palette, so the theme button — live exactly when the static figure is showing, because `initTheme` runs before the try — inverts it; `color-scheme` is never declared; `display:block` on `<table>` drops the table role; `#main` lacks `tabindex="-1"`, which defeats the skip link in Safari; and `plot.js`'s `fmt` writes `×10^-9` where `ui.js` writes proper superscripts, leaving the widest y-tick about 3 px from the edge at 358 px. Measured: `--line` is 2.99:1 on `--bg` and 3.81:1 in dark, so the open D6 item is marginal, not the 1.35-1.54:1 it records. Card nits: theta.json holds 154 records, not 153; the Richardson table is six ladders over FIVE families, as the page says and the card does not; and the plan card's P4a note that `docs/index.html` lists only modules 1, 2 and 6 is now stale — it links all three.
