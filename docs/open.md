<!-- Generated by tools/build_docs.py from the knowledge base. Do not edit by hand: run `make docs`. -->

# What is open

Every `next:` field below is quoted verbatim from its knowledge-base card: this page records the concrete next step, not an aspiration. Project-management cards (paper edits, plans, infrastructure) are deliberately left out.

## Open questions (5)

### The universal corner-correlation kernel Ghat(Delta): closed form, positivity, decay

`open` · knowledge-base id `corner-correlation-kernel`

**next:** G1b + G3 in parallel.

### Certify the defect-matrix quadrature and the box cutoff

`open` · knowledge-base id `dhat-quadrature-certification`

**next:** Interval-arithmetic 2-D quadrature of the defect kernel on the box frame, and a rigorous bound for the modes beyond kappa_max (box asymptotics 0.0641 zeta^2/kappa_max^2).

### Multi-interval networks (O10, second half)

`open` · knowledge-base id `multi-interval-networks`

**next:** Phase 6 (rigor/phase6_brief.md, card phase6-plan): corner calculus (G1a), second-order network law with the universal kernel Ghat (G1b), lattice sequential Petz chains and the VWZ protocol test 1:2:4 (G2), Ghat(Delta) in two frames (G3).

### Charged sectors, state dependence, collar Gaussianity (O11)

`open` · knowledge-base id `sectors-o11`

**next:** Sector-dependent Markov defect for the free fermion (quasi-free or finite rank); write the half-page collar-Gaussianity lemma.

### theta as a geometry-free Wiener-Hopf constant in the modular frame of I

`open` · knowledge-base id `theta-modular-wiener-hopf`

**next:** Solve or bound the Wiener-Hopf problem of Prop 6.2 and compare with theta = 0.384 +- 0.003 (F3 is done); a closed form is open, and no candidate is to be fitted to the numerics

## Conjectures (3)

Believed, with partial evidence, and not proved.

### [s2 = c/12 for general nets](results/conj-s2.md)

`conjectural` · knowledge-base id `conj-s2`

**next:** Renormalised/collar route for a gauge lemma modulo the commutant orbit; then the exact identity applies. Until then paper 2 states s2 = c/12 as a conjecture with the bracket.

### [Second-order network law: Phi_tot = sum zeta_j zeta_k G(y_j - y_k), G = c Ghat universal](results/network-second-order-law.md)

`conjectural` · knowledge-base id `network-second-order-law`

**next:** G1b proof; G3 numerics; then compare with the lattice (G2).

### [VWZ four-interval protocols: Phi(1):Phi(2):Phi(3) -> 1:2:4; middle-out is the worst order](results/vwz-protocol-ordering.md)

`conjectural` · knowledge-base id `vwz-protocol-ordering`

**next:** G2 run; G1a proof of the exact reductions.

## Settled claims that still record a next step (4)

These carry a status of their own; the step recorded is what would sharpen or extend them.

### [E_rec \<= (1-theta) f2 z^2 (1+o(1)): the zero-collar compression is NOT optimal in exact fidelity](results/compression-not-optimal-exact-fidelity.md)

`numerical` · knowledge-base id `compression-not-optimal-exact-fidelity`

**next:** kappa_opt \<= 0.616 +- 0.003; whether non-isometric channels lower it further is the subject of Phase 5 -- the KKT criterion's endpoint condition is still being evaluated (F1/F2), numerically no (card kkt-noise-criterion-numerics). Do not record kappa_opt = 1 - theta as settled.

### [Corner calculus: sequential one-sided recovery = Schwarzian point masses at the junctions](results/corner-calculus.md)

`proved` · knowledge-base id `corner-calculus`

**next:** G2 to test the general rule (Thm 5.2) on the lattice, including union-conditioning and single-interval-start protocols where (H) fails.

### [Is the compression beaten in EXACT fidelity?](results/exact-fidelity-optimality-open.md)

`numerical` · knowledge-base id `exact-fidelity-optimality-open`

**next:** Remaining: the exact value of the all-channel optimum (non-isometric channels; bracket \[D_z, (0.616 +- 0.003) f2 z^2\]) and an analytic proof that E_D u E_D is not self-adjoint (theta > 0). kappa_opt = 1 - theta is NOT settled: the KKT criterion's endpoint condition is still being evaluated (Phase 5, F1/F2); numerically non-isometric channels do not lower it further (card kkt-noise-criterion-numerics).

### [KKT sign criterion: when the isometric orbit carries the all-channel second-order optimum](results/kkt-noise-sign-criterion.md)

`proved` · knowledge-base id `kkt-noise-sign-criterion`

**next:** F2 evaluates the endpoint number E(zeta_0) of condition (e) in the rigid frame; (a) is numerically settled (F2 T4)

---

[Documentation map](index.md) · [All claims by area](status.md)
